# Backend API base URL (contact form + GitHub contributions proxy).
# - Dev: leave empty to use the Vite proxy at /api
# - Production: set to your deployed Express backend (Railway/Render/Fly).
#   Do NOT set this to your Vercel frontend URL — projects load from bundled data.
VITE_API_URL=
