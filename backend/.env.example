NODE_ENV=development
PORT=5000
# Production: set to your deployed frontend URL (e.g. https://samirj.vercel.app)
CLIENT_ORIGIN=http://localhost:5173
GITHUB_USERNAME=samirzjadhav
# Optional: increases GitHub API rate limits (leave unset or omit — do not use an empty value)
# GITHUB_TOKEN=
